# Headless CMS 仕様書 (V1)
github: https://github.com/JoHyemi/headless-cms-template

# 1. 概要

## 1.1 目的

本システムは、企業Webサイトのコンテンツを管理するための
Headless CMS(Content Management System)です。

管理画面から記事・画像・ページ情報を登録し、その内容を公開Webサイトへ表示できます。

一般的なCMSとは異なり、コンテンツ管理機能(CMS)と公開Webサイトを分離した構成を採用しています。

これにより、以下を実現します。

- コンテンツ更新の効率化
- Webサイト変更時の柔軟性向上
- 開発者に依存しない運用
- 将来的なサービス展開への対応

---

# 2. 解決する課題

## 従来のWebサイト運用における課題

- Webサイト更新のたびに開発者対応が必要
- ページごとにデザイン品質がばらつく
- CMS変更時にWebサイト全体へ影響する
- 同じコンテンツを別サービスで利用しにくい

## 本CMSによる改善

- 管理者自身でコンテンツ更新が可能
- 決められたパーツでページ品質を維持
- WebサイトとCMSを独立管理
- APIを利用した将来的な拡張が可能

---

# 3. システム構成

```
[管理画面(Admin)]
        |
        ↓
    [CMS API]
        |
        ↓
[公開サイト(Website)]
```

## 3.1 管理画面(Admin)

コンテンツ管理者が利用するCMS画面です。

主な機能:

- 記事作成・編集
- カテゴリー管理
- 画像管理
- コンテンツ公開管理
- ブロック管理

管理者はWebブラウザからログインし、
専門的な開発知識なしでコンテンツを更新できます。

---

## 3.2 CMS API

CMS全体のデータ管理を担当するシステムです。

役割:

- コンテンツ保存
- コンテンツ提供
- 管理画面とWebサイトの連携
- 外部サービス連携基盤

管理画面と公開サイトはデータベースへ直接アクセスせず、必ずAPIを経由します。

---

## 3.3 メディア管理

アップロードされたファイルを一元管理できます。

対応機能:

- 画像アップロード
- 画像削除
- 画像再利用
- Altテキスト管理
- キャプション管理

メリット:

- 画像管理効率化
- SEO対応
- アクセシビリティ対応
- 重複アップロード防止

---

# 4. データモデル

## 4.1 主要エンティティ

|エンティティ|説明|
|-|-|
|Post|記事。タイトル・slug・本文(Block配列)・カテゴリー・公開状態を持つ|
|Category|カテゴリー。1つの記事が複数カテゴリーに属することが可能|
|Media|アップロードされた画像ファイル。URL・Alt・キャプションを保持|
|BlockType|カスタムブロックの設計図。名前・スラッグ・フィールドスキーマ(Json)を保持|

## 4.2 公開状態

記事は DRAFT / PUBLISHED の2状態を持つ。
DRAFT は管理画面にのみ表示され、API・公開サイトには表れない。

---

# 5. Block Editor仕様

## 5.1 標準ブロック

|ブロック|説明|
|-|-|
|段落|基本テキスト|
|見出し|H2/H3|
|リスト|順序なし/順序付き|
|引用|引用文+出典|
|画像|URL・Alt・キャプション|
|画像ギャラリー|複数画像。個別にAlt/キャプション設定、並べ替え可|
|カスタムブロック|管理画面で定義した独自ブロック(6章参照)|

## 5.2 データ形式

本文はブロックの配列(Json)として保存する。各ブロックは `type` フィールドで
種別を判定し、レンダリング時にブロックごとの専用コンポーネント/HTML生成関数へ
振り分ける。

## 5.3 出力形式

APIレスポンスには、生のブロック配列(`content`)と、サーバー側で事前生成した
完成済みHTML(`contentHtml`)の両方を含む。用途に応じて選択して利用可能。

---

# 6. Custom Block仕様(拡張機能)

## 6.1 目的

WordPressのACF(Advanced Custom Fields)と同様、コード変更なしに管理画面から
独自ブロックを追加できる仕組み。

## 6.2 設計方針

ブロック定義には生のHTMLテンプレートを保存せず、フィールドスキーマ(項目の
設計図)のみを保存する。実際の見た目は、あらかじめ登録されたレンダラー
(スラッグ単位)が担当する。これにより自由度と安全性を両立する。

## 6.3 BlockType定義項目

|項目|説明|
|-|-|
|name|ブロックタイプの表示名|
|slug|一意の識別子。レンダラーとの紐付けに使用|
|fields|フィールド配列。各フィールドは key/label/type/required を持つ|

対応フィールドタイプ: text / textarea / image / boolean / number

## 6.4 レンダリング

|状況|表示|
|-|-|
|登録済みスラッグ(例: cta-button)|専用デザインで描画|
|未登録スラッグ|フィールド値を安全にエスケープした標準フォールバック表示|

## 6.5 バリデーション

必須フィールドが未入力の場合、保存時にエラーを表示し保存を中止する
(該当ブロック名・フィールド名を明示)。

---

# 7. API仕様

## 7.1 認証

ログイン(`POST /auth/login`)成功時にhttpOnly Cookie(JWT)を発行。
書き込み系エンドポイント(POST/PATCH/DELETE)は認証必須。

## 7.2 主要エンドポイント

|エンドポイント|認証|説明|
|-|-|-|
|POST /auth/login|不要|ログイン|
|POST /auth/logout|不要|ログアウト|
|GET /posts|不要|公開済み記事一覧|
|GET /posts/slug/:slug|不要|記事詳細(公開済みのみ)|
|GET /posts/all|必要|全記事一覧(下書き含む)|
|POST /posts|必要|記事作成|
|PATCH /posts/:id|必要|記事更新|
|DELETE /posts/:id|必要|記事削除|
|GET /categories|不要|カテゴリー一覧|
|GET/POST/PATCH/DELETE /media|必要|メディア管理|
|GET/POST/PATCH/DELETE /block-types|必要|カスタムブロック定義管理|

## 7.3 レスポンス形式

JSON形式。一覧系は `{ "posts": [...] }` のように配列をラップして返す。

---

# 8. セキュリティ

## 8.1 XSS対策

本文は自由なHTML入力を許可せず、決められたブロック形式でのみ保存する。
表示時はすべての文字列を安全にエスケープしてから描画するため、本文に
悪意のあるコードが入力されても実行されない。

## 8.2 認証・認可

書き込み系APIはすべてJWT認証必須。セッションはhttpOnly Cookieで管理し、
フロントエンドのJavaScriptから直接読み取れない。

## 8.3 Custom Blockの安全性

カスタムブロックはHTMLテンプレートではなくフィールドスキーマのみを保存する
ため、ブロック定義自体が任意コード実行の経路にならない。未登録スラッグは
安全なフォールバック表示となり、表示が壊れることはあっても、コードが実行
されることはない。

---

# 9. URL構成(予定)

本番環境では役割ごとにURLを分離します。

|用途|URL例|
|-|-|
|管理画面(Admin)|https://admin.company.jp|
|API|https://api.company.jp|
|公開サイト(Website)|https://company.jp|

各サービスは独立したアプリケーションとして運用します。

---

# 10. 従来型CMSとの違い

|項目|一般的なCMS|本CMS|
|-|-|-|
|管理画面|Webサイトと一体型|完全分離|
|デザイン変更|CMSへの影響あり|Webサイト側で変更可能|
|入力方式|自由HTML入力|Block方式|
|拡張方法|テーマ・Plugin依存|フィールドスキーマベースのカスタムブロック(API経由で定義)|
|利用範囲|1サイト中心|複数サービス展開可能|

---

# 11. V1対応範囲

## 対応済み機能

✅ 管理者ログイン

✅ 記事管理

✅ カテゴリー管理

✅ メディア管理

✅ Block Editor

✅ Custom Block

✅ API提供

✅ 公開Webサイト連携

---

# 12. 今後検討する機能

本番運用レベルへ拡張する場合、以下の機能を検討します。

|項目|目的|
|-|-|
|ユーザー権限管理|複数担当者で安全に利用|
|承認Workflow|公開前チェック|
|Version History|過去内容への復元|
|予約公開|指定日時で公開|
|SEO設定|検索対策|
|多言語対応|海外サイト展開|
|検索機能|大量コンテンツ対応|
|操作ログ|変更履歴確認|
|Backup強化|障害対策|
|SSO連携|企業認証対応|

---

# 13. 本番運用前の確認事項

|項目|確認内容|
|-|-|
|CMS利用者|何名が利用するか|
|権限設定|管理者・編集者などの役割|
|公開フロー|作成者・確認者・公開者|
|画像保存方式|Storage方式|
|Backup|保存期間・復旧手順|
